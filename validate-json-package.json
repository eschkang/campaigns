{
  "name": "validate-json",
  "version": "1.0.0",
  "description": "Validate all scenario JSON files before commit.",
  "scripts": {
    "lint:json": "npx --yes jsonlint-cli '**/*.json' -q"
  },
  "devDependencies": {
    "jsonlint-cli": "^1.0.0"
  }
}
