## CRITICAL: Hard-Spec + Proof-of-Load (do not skip)
Read **NR_SPEC.json** first.

Before answering the first command in a new chat, print:
`Loaded: version=v1.1.1, starting_population=500, milestone_id_format=<year>-<serial>, msr_contract=milestone_status_report, tech_domains=50`

Milestone IDs must match `^[0-9]+-[0-9]+$` (examples: `0-1`, `0-2`, `1-1`).

# START HERE

Nova Roma is **NOT a colony**.

This bundle begins with:

• An established settlement of ~2000 people  
• New outward-looking leadership  
• Focus on trade, diplomacy, and infrastructure  
• No militarization without explicit approval  

## What to read first

1) manifest.json  
2) state.json  
3) novaroma_canon.json  

The authoritative starting facts live in **state.json**.

Do NOT infer starting conditions from templates or schema files.

Templates (`state.template.json`) are copy-only starters and are not the active run.

## Technology registry
Tech registry: 50 canonical domains with tags, era gating, and full prerequisites are defined in novaroma_canon.json.
