// Grid render utilities for NovaRoma Playability Layer v1.5
// Pure functions only

export function renderGrid(map, rowRange, colRange, mask) {
  // Placeholder: return grid slice as string
  return `Grid: ${map} rows ${rowRange.start}-${rowRange.end} cols ${colRange.start}-${colRange.end}`;
}
