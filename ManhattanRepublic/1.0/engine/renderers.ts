// Report renderers for NovaRoma Playability Layer v1.5
// Pure functions only

export function renderStatusReport(entity, date) {
  return `Status — ${entity}\nDate: ${date}`;
}

// TODO: Implement renderers for site report, milestones report, trackers
